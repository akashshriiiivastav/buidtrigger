pipeline {
    agent any
    stages {
        stage("Build Trigger") {
            steps {  // "step" should be "steps"
                sh "echo 'Hello World'"  // Correct the quote usage
            }
        }
    }
}
